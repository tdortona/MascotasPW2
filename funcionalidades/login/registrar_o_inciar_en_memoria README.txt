*registro de nombre de usuario y contraseña
*solo se almacena en sesion, dentro de un array
*se utiliza el metodo post
*se crea un objeto "cuenta" apartir de una clase
*solo validad que no esten los 2 campos vacios
*verifica que no exita un usuario con el mismo nombre
*no permite ingresar a las paginas "registro" e "inicio" directamente por url, verifica si $_post fue seteado
